spring:
  cloud:
    service-registry:
      auto-registration:
        enabled: false

  datasource:
    url: jdbc:mysql://127.0.0.1:3306/mini_app?characterEncoding=utf8&useSSL=false&autoReconnect=true&failOverReadOnly=false&maxReconnects=10&serverTimezone=GMT%2B8
    username: guest01
    password: 123qwe
    driver-class-name: com.mysql.jdbc.Driver

  # Redis
  cache:
    type: redis
  redis:
    host: peer1
    port: 6379
    timeout: 10000
    database: 0
    lettuce:
      pool:
        max-active: 8
        max-wait: -1
        max-idle: 8
        min-idle: 0
