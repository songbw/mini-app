
spring:
  datasource:
    url: jdbc:mysql://122.112.211.218:33143/ztkx_miniapp?characterEncoding=utf8&useSSL=false&autoReconnect=true&failOverReadOnly=false&maxReconnects=10&serverTimezone=GMT%2B8
    username: fengchao
    password: Smartautotech@123
    driver-class-name: com.mysql.jdbc.Driver

  jackson:
    time-zone: GMT+8

  cloud:
    config:
      enabled: false
      discovery:
        enabled: false

  # Redis
  cache:
    type: redis
  redis:
    host: peer1
    port: 6379
    timeout: 10000
    database: 0
    lettuce:
      pool:
        max-active: 8
        max-wait: -1
        max-idle: 8
        min-idle: 0

logging:
  file: /data/logs/miniApp/miniApp.log
  pattern:
    file: '%d %-5level [%thread] %logger : %msg%n'
  level:
    com.fengchao.miniapp.controller: INFO
    com.fengchao.miniapp.service.impl: INFO
    com.fengchao.miniapp.config: INFO

management:
  loggers:
    enabled: true
    sensitive: false
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS
